import React, {useState} from 'react';
import {HeaderContainer} from '../../styles/presentationals/HeaderStyles'
import Link from "next/link"

const Header = (props) => {
  return(
    <HeaderContainer>header</HeaderContainer>
  )
}

export default Header;